{
  "name": "kinetic-oss",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "catalog:publish": "node scripts/publish-catalog.mjs",
    "check": "node scripts/check-catalog.mjs && pnpm --recursive --if-present check"
  }
}
